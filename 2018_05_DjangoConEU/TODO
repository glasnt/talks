[x] fields per model

[x] Model table
[x] Codepoint._meta.db_table

[x] for c in Codepoint._meta.get_fields():
    c.name, c.is_relation

Clear up WHERE clause stuff
  Go from Russell's email


Ensure that the words and tone are cleaned up in practice

Q bitwise operations
 - include overloaded operand code references?
